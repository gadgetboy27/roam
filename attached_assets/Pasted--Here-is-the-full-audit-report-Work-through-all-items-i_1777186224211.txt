

Here is the full audit report. Work through all items in priority order starting with the remaining Criticals, then High, then Medium. Fix one at a time, run the test suite after each change, and confirm it passes before moving to the next.
CRITICAL — fix first:

C1: Add io.use() auth middleware to Socket.IO — verify Bearer token, store userId on socket.data.userId, replace all data.senderId references
C2: Replace open Supabase RLS policy on messages and typing_indicators with participant-scoped policies
C3: Remove hardcoded Supabase URL and anon key fallbacks from client/src/lib/supabase.ts
C4: Run drizzle-kit generate then drizzle-kit migrate to add missing columns (ticket_paid, ticket_session_id, ticket_price_nzd, boost_expires_at, is_organiser, stripe_connect_account_id, stripe_connect_onboarded)
C5: Replace in-memory rate limiter Maps with express-rate-limit backed by the existing Postgres session pool

HIGH — fix next:

H1: Wrap /discover, /groups, /groups/:id, /roamers, /whats-on, /plans in <RequireAuth> in client/src/App.tsx
H2: Wrap /admin and /admin/ads routes in an admin auth guard component
H3: Make users.password nullable in schema, add auth_provider column, disable legacy /api/auth/login path
H4: Install Helmet, add Content-Security-Policy header in server/index.ts
H5: Add all missing database indexes in a new migration (photos.user_id, matches.user_a/b_id, messages.match_id, notifications.user_id, group_members, ads.status, users.stripe_customer_id)
H6: Eliminate N+1 query in /api/discover — use existing getFirstApprovedPhotoPerUser() instead of per-user photo fetches, add pagination
H7: Add FK constraints to schema using Drizzle .references() with onDelete: cascade
H8: Remove app.use("/uploads", express.static(...)) from server/index.ts

MEDIUM — fix after:

M1: Replace "2025-08-27.basil" as any Stripe API version with the stable typed version
M3: Add explicit guard — if (!process.env.SESSION_SECRET) throw new Error(...)
M4: Add startup guard for STRIPE_PAYMENT_WEBHOOK_SECRET
M5: Remove unused passport and passport-local packages
M6: Remove unused memorystore package
M7: Move app.set("trust proxy", 1) to server/index.ts before listen()
M8: Add 2000-character max length validation on message content
M9: Add server-side max-length validation on caption, tagline fields
M10: Create admin_audit_log table, log all admin actions
M11: Make supabase_setup.sql idempotent with DROP POLICY IF EXISTS
M12: Add HSTS header (resolved by Helmet in H4)

Also fix this bug found during test writing (not in the original report):

server/auth.ts comparePassword() throws a TypeError instead of returning false when the stored hash has no colon separator. Wrap the body in try/catch and return false on error. The admin-auth.ts version already does this correctly — match that pattern.
Add .max(128) to the password field in signupSchema to prevent DoS via expensive scrypt on very long inputs.